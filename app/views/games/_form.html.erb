<%= simple_form_for @game, html: {class: "form-horizontal"} do |f| %>
  <%= f.input :date %>
  <%= f.association :golf_course, collection: GolfCourse.order(:name), prompt: "Choose a Course" %>
  <%= f.submit class: "btn btn-primary" %>
<% end %>
